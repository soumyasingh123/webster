<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Poppins', sans-serif;
        }

        body {
            background-color: #FFF0D1;
            background-position: center;
            color: #3B3030;
            padding-bottom: 50px;
        }

        header {
            background-color: transparent;
            position: static;
            width: 100%;
            padding: 20px 100px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            z-index: 99;
        }

        .logo {
            font-size: 2rem;
            color: #3B3030;
            user-select: none;
        }

        .navigation a {
            font-size: 1.1rem;
            color: #3B3030;
            text-decoration: none;
            font-weight: 500;
            margin-left: 40px;
            position: relative;
        }

        .navigation a::after {
            content: '';
            position: absolute;
            left: 0;
            bottom: -6px;
            width: 100%;
            height: 3px;
            background: #3B3030;
            border-radius: 5px;
            transform-origin: right;
            transform: scaleX(0);
            transition: transform .5s;
        }

        .navigation a:hover::after {
            transform-origin: left;
            transform: scaleX(1);
            transition: transform .5s;
        }

        .back_button {
            width: 130px;
            height: 50px;
            background: transparent;
            border: 2px solid #3B3030;
            border-radius: 6px;
            cursor: pointer;
            font-size: 1.1rem;
            color: #3B3030;
            font-weight: 500;
            margin-left: 40px;
            transition: .5s;
        }

        .back_button:hover {
            background: #3B3030;
            color: #FFF0D1;
        }

        .row {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .image_port {
            display: flex;
            flex-direction: row;
            align-items: center;
            gap: 10px;
        }

        .image_port img {
            width: 200px;
            height: 200px;
            border-radius: 50%;
            object-fit: cover;
            opacity: 0.8;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .row .form_box {
            margin-top: 20px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            background-color: transparent;
            border-radius: 5px;
            width: 600px;
            height: auto;
            display: flex;
            flex-direction: column;
            padding: 20px;
            font-size: larger;
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 5px;
            font-weight: bold;
        }

        input[type="text"],
        input[type="number"] {
            width: 100%;
            padding: 8px;
            border: 1px solid #3B3030;
            border-radius: 4px;

        }

        .form-button {
            padding: 10px;
            width: 20%;
            border: none;
            background-color: #3B3030;
            color: white;
            font-size: 16px;
            border-radius: 4px;
        }
    </style>
</head>

<body>
    <header>
        <h2 class="logo">Logo</h2>
        <nav class="navigation">
            <a href="/explore">Home</a>
        </nav>
    </header>

    <div class="row">
        <div class="image_port">
            <img id="profilePhoto" src=<%= userDetails.avatar %> alt="Profile Photo">
            <a href="/profile/avatar"><button class="back_button">Change Avatar</button></a>
            <a href="http://localhost:7000/portfolio/<%= userDetails.id %>"><button class="back_button">My Portfolio</button></a>       
        </div>
        <div class="form_box">
           <form>
                <div class="form-group">
                <label for="Username">Username</label>
                <input type="text" id="NameInput" readonly placeholder="<%= userDetails.username %>">
                </div>
                <div class="form-group">
                    <label for="battleInput">Email</label>
                    <input type="number" id="battleInput" placeholder="<%=userDetails.email %>" readonly>
                </div>
                <div class="form-group">
                    <label for="scoresInput">Scores</label>
                    <input type="number" id="scoresInput" placeholder="<%= userDetails.points %>" readonly>
                </div>
            </form>
        </div>
    </div>
</body>

</html>